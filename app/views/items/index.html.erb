<div id="page_title" style="text-align: center">
  <h1>Nos chatons trop mignons</h1>
</div>
<div class="container mt-4">
  <div class="row">
    <% @items.each do |item| %>
      <div class="col-md-4 mb-4 carrejaune_accueil div">
        <div class="card">
          <%= link_to item_path(item) do %>
            <img src="<%= item.image_url %>" class="card-img-top img-fluid">
          <% end %>
          <div class="card-body">
            <h2 class="card-title text-center"><%= item.title %></h2>
            <div class="d-flex justify-content-around" id="div-a-masquer">
              <%= link_to "Voir en détails", item_path(item), class: "btn btn-success" %>
              <%= button_to "Ajouter au panier", add_to_cart_item_path(item.id), remote: true, method: :post, form_class: "form-inline", class: "btn btn-warning add-to-cart", data: { item_id: item.id } %>
            </div>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>